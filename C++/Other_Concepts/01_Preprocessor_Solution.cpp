#include <climits>
#define INF INT_MAX
#define toStr(x) #x
#define io(x) cin >> x
#define FUNCTION(name,op) inline void name(int &x, int cp) {if(cp op x)x = cp;} 
#define foreach(v ,i) for(int i = 0; i < v.size(); i++)
